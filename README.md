# ruleevaluator

A lightweight, zero-dependency Go library for evaluating dynamic expressions against arbitrary data structures, with support for nested field access, custom functions, and variable substitution.

## Setup

See [docs/PROJECT_BRIEF.md](docs/PROJECT_BRIEF.md) for project context.

## Agent sessions

This repo uses the [agentic development framework](https://github.com/eddiecarpenter/ai-native-delivery).
See `base/AGENTS.md` and `AGENTS.local.md` for session protocols.
